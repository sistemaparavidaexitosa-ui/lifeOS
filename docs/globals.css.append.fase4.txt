/* ----------------------------------------------------------------------- */
/* Tree View (FASE 4 — vista tipo explorador de archivos)                   */
/* Reutiliza tokens ya existentes (--line, --surface2, --muted, --c-*,      */
/* --st-*) — cero colores nuevos, mismo Design System.                      */
/* ----------------------------------------------------------------------- */
.tree-view {
  display: flex;
  flex-direction: column;
  gap: 16px;
  padding: 12px;
}
.tree-group {
  border: 1px solid var(--line);
  border-radius: 10px;
  overflow: hidden;
  transition: background 0.12s ease;
}
.tree-group-dragover {
  background: var(--surface2);
}
.tree-group-head {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 10px;
  border-left: 4px solid var(--c-purple);
  background: var(--surface);
}
.tree-group-name {
  background: transparent;
  border: none;
  font-weight: 700;
  font-size: 14px;
  min-height: auto;
  padding: 2px 4px;
  flex-shrink: 0;
  width: auto;
  max-width: 240px;
}
.tree-group-name:focus {
  background: var(--surface2);
  border-radius: 6px;
}
.tree-node {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 6px 10px;
  border-top: 1px solid var(--line);
  cursor: grab;
}
.tree-node:active {
  cursor: grabbing;
}
.tree-node-dragover {
  background: var(--surface2);
  outline: 1px dashed var(--accent-d);
}
.tree-node-title {
  background: transparent;
  border: none;
  text-align: left;
  padding: 2px 4px;
  min-height: auto;
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.tree-node-title:hover {
  text-decoration: underline;
}
.tree-progress {
  position: relative;
  display: inline-flex;
  align-items: center;
  width: 64px;
  height: 14px;
  border-radius: 7px;
  background: var(--surface2);
  overflow: hidden;
  flex-shrink: 0;
}
.tree-progress-bar {
  position: absolute;
  inset: 0 auto 0 0;
  background: var(--st-done, #2e7d32);
  border-radius: 7px;
}
.tree-progress-label {
  position: relative;
  z-index: 1;
  font-size: 9px;
  font-weight: 700;
  margin: 0 auto;
  color: var(--text);
  mix-blend-mode: difference;
}
.tree-add-group {
  display: flex;
  gap: 6px;
  align-items: center;
  padding: 4px 10px;
}
.tree-add-group input {
  flex: 1;
}
